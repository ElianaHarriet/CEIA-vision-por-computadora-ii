# Minimal requirements for Airflow without heavy ML dependencies
mlflow[extras]~=2.10
boto3~=1.34
roboflow~=1.1
Pillow~=10.0
psycopg2-binary~=2.9

# Evaluation Libraries (lightweight)
matplotlib>=3.7.0
scikit-learn>=1.3.0
scipy>=1.11.0

# NOTA: Las librerías de training (torch, ultralytics, etc.) se instalan
# solo cuando se ejecutan los DAGs de entrenamiento, no en la imagen base
