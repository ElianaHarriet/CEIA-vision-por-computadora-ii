mlflow[extras]~=2.10
boto3~=1.34
roboflow~=1.1
Pillow~=10.0
psycopg2-binary~=2.9

# ML Training Libraries
# torch/torchvision se instalan aparte (CPU-only, ver Dockerfile) porque el
# training real corre en RunPod; acá solo se usan para la validación local
# de YOLO en CPU.
ultralytics>=8.0.0
opencv-python-headless>=4.8.0
tqdm>=4.65.0

# Evaluation Libraries
matplotlib>=3.7.0
scikit-learn>=1.3.0
scipy>=1.11.0
